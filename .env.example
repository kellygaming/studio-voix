# --- Front (Vercel) ---
NEXT_PUBLIC_SUPABASE_URL=https://xxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Côté serveur uniquement (routes API) : ne jamais préfixer par NEXT_PUBLIC_
ELEVENLABS_API_KEY=
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# --- Worker (Railway / Render / Fly.io) : voir worker/.env.example ---

# --- Mesure d'audience et conversions publicitaires (optionnel) ---
# Laisser vide = aucun script de tracking n'est chargé.
NEXT_PUBLIC_GA_ID=
NEXT_PUBLIC_META_PIXEL_ID=
